fn main() {
    // Setup Vec
    let mut v = vec![1, 2];
    v = process(v);
    drop(v);
    return;
}

fn process(v: Vec<_>) {
    // p is a reference to the vec in the
    // main's stackframe
    v.push(42);
    return v;
}
