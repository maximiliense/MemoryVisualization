fn main() {
    let arr: [i32; 5] = [2, -3, 5, 12, 0];
    let arr_len: i32 = 5;
    let max: i32 = arr[0];
    let i: i32 = 1;
    while i < arr_len {
        if arr[i] > max {
            max = arr[i];
        }
        i += 1;
    }
    print!("Max value: ");
    println!(max);
    return;
}
