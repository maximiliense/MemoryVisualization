fn main() {
    // Compute v = n!
    let n: i32 = 5;
    let v = fac(n);
    print!("factorial=");
    println!(v);
    return;
}

fn fac(n: i32) {
    let res: i32;
    if n == 1 {
        res = n;
    } else {
        res = fac(n - 1);
        res *= n;
    }
    return res;
}
