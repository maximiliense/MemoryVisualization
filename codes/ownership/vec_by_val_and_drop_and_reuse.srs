fn main() {
    // setup vec
    let v = vec![1, 2];
    process(v);
    v.push(42);
    return;
}

fn process(v: Vec<i32>) {
    v.push(42);
    // On fait des choses avec v
    println!(v);
    v.push(64);
    return;
}

