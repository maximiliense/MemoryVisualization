fn main() {
    // setup vec
    let v = vec![1, 2];
    process(v);
    // Interdit de réutiliser v ici
    return;
}

fn process(v: Vec<i32>) {
    v.push(42);
    // On fait des choses avec v
    drop(v);
    return;
}
