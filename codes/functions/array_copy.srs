fn main() {
    let arr: [i32; 5] = [1, 2, 3, 5, 6];
    arr = modify(arr);
    return;
}

fn modify(arr: [i32; 5]) {
    arr[0] = 9999;
    // We must return arr to propagate
    // the modification
    return arr;
}
