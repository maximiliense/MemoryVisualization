fn main() {
    // setup
    let x: i32 = 10;
    let y: i32 = 10;
    // Calling process with x and y
    process(x, y);
    return;
}

fn process(x: i32, y: i32) {
    // x and y are copies of their main's counter part
    x += 1;
    y += 1;
    return;
}
